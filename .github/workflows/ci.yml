name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.event.pull_request.number || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  backend-tests:
    name: Backend tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run backend tests
        run: npm test --workspace apps/backend

  frontend-quality:
    name: Frontend quality
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Check frontend types
        run: npm run typecheck --workspace apps/frontend

      - name: Lint frontend
        run: npm run lint --workspace apps/frontend

      - name: Run frontend tests
        run: npm test --workspace apps/frontend

      - name: Check Expo dependency matrix
        run: npm run expo:check --workspace apps/frontend

      - name: Run Expo Doctor
        run: npm run expo:doctor --workspace apps/frontend

      - name: Export Expo Web app
        env:
          EXPO_PUBLIC_API_URL: https://api.ci.invalid/v1
        run: npm run build --workspace apps/frontend

  frontend-delivery:
    name: Frontend container delivery
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: frontend-quality
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Test Expo Web container
        run: npm run test:delivery --workspace apps/frontend

  build:
    name: Build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build backend
        run: npm run build --workspace apps/backend
