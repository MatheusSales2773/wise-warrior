# Values prefixed with EXPO_PUBLIC_ are embedded in the client bundle.
# Do not put secrets or private keys in these variables.
# Web and iOS Simulator can reach localhost. Android Emulator uses 10.0.2.2;
# physical devices need an HTTPS endpoint or the development machine's LAN IP.
EXPO_PUBLIC_API_URL=http://localhost:3000/api/v1
